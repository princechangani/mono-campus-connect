package com.monocampusconnect.repository.postgres;

import com.monocampusconnect.model.postgres.FacultyWorkExperience;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FacultyWorkExperienceRepository extends JpaRepository<FacultyWorkExperience, Long> {
}

